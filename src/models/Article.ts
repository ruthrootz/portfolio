class Article {
    public title: string;
    public publishDate: Date;
    public text: string;
  
    constructor(title: string, publishDate: Date, text: string) {
      this.title = title;
      this.publishDate = publishDate;
      this.text = text;
    }

}